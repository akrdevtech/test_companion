export enum ErrorSource {
  INTERNAL = 'Internal',
  VALIDATION = 'Validation',
  CONFIGURATION = 'Configuration',
  AWS = 'AWS',
  DB = 'Database',
  COGNITO = 'Cognito',
  EVENT_BRIDGE = 'Event Bridge',
  NOT_FOUND = 'Not Found',
}
